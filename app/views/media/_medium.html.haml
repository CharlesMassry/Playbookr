= div_for(medium) do
  = render medium.content
  = medium.caption
  .media_comment
    = button_tag "Add comment to media", class: "new_media_comment", data: { type: "medium", id: medium.id }
  = render medium.comments
