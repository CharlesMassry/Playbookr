$("#medium_<%= @comment.commentable_id %>").append("<%=j render(@comment) %>");
var button = $("<button class='new_media_comment' data='<%= @comment.commentable_id %>' name='button' type='submit'>Add comment to media</button>");
$("form#medium_comment_form_<%= @comment.commentable_id %>").replaceWith(button);
